//alert('Hello')

//Declare two variable num1, num2 and assign 10 and 20 respectively
//Add num1 and num2 and move the resultant into another variable called result

//All JavaScript variables must be identified with unique names.

//These unique names are called identifiers.

num1 = 30;
num2 = 5;

add = num1 + num2;
sub = num1 - num2;
mul = num1 * num2;
div = num1 / num2;

//Get the span tag using its id demo and move the value of the result in it  

document.getElementById('demo').innerHTML = add;
document.getElementById('demo1').innerHTML = sub;
document.getElementById('demo2').innerHTML = mul;
document.getElementById('demo3').innerHTML = div;